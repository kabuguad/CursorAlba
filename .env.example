# API Configuration
# Set this to your backend API base URL (ngrok tunnel, local server, etc.)
# The Vite dev server proxies all /api/* requests to this target.
VITE_API_BASE=https://your-api-url.ngrok-free.dev
